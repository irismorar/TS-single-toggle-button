#SINGLE TOGGLE BUTTON

The application has one button called "Toggle me!".

The button can be toggled "on" or "off".

When the button is "on", the backgound color of the button is orange. When the button is "off" the background color of the button is grey.

Initially, the button is "off". You have a maximum limit of 10 toggles.
